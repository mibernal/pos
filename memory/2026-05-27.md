# 2026-05-27 Daily Note

## Summary of Changes
- **Redis Mocking**: Implemented a stateful, in-memory Redis mock client for the API tests in `apps/api/src/app/build-app.ts`. Previously, calling `redis.incr` or `redis.expire` inside `recordLoginRateLimitFailure` during failed login rate limit checks threw a TypeError (returning 500 status code) because they were not implemented in the test Redis stub.
- **E2E Schema Initialization**: Updated `ensureE2eSchema` in `apps/api/test/helpers/e2e-fixture.ts` to create the newer tables/columns from migrations 10 to 17 (specifically `product_variants`, `promotions`, `sale_returns`, `return_items`, `allow_negative_stock`, `min_stock_alert_qty`, and `variant_id` on `sale_items`). Previously, these missing tables/columns caused PG errors (returning 500 status code) in E2E tests (`system-flow.e2e.test.ts`).
- **Fake DB Setup**: Added `product_variants` to the `FakeDbState` and `createFixture` in `apps/api/test/sales-create-fiscal.test.ts`. This resolves potential type mismatch and undefined state issues when simulating variant lookups in Kysely tests.

## Results
- The changes address all 12 failing tests in the API test suite (across `auth-rate-limit.e2e.test.ts`, `sales-create-fiscal.test.ts`, and `system-flow.e2e.test.ts`) that were failing with a `500` status code.
